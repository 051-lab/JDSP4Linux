#!/usr/bin/env bash
set -euo pipefail

printf '%s\n' "$*" >> "$PACKAGING_FAKE_LOG"
if [[ "${PACKAGING_FAKE_FAIL_BUILD:-0}" == 1 && "${1:-}" == build ]]; then
  exit 1
fi
if [[ "${1:-}" == run ]]; then
  exit 0
fi
